query StudentArrivalNotice($studentId: UUID) {
    studentArrivalNotice(studentId: $studentId) {
        id
        studentId
        profession
        address
        date
        expires
        invitingSide
        receivingSide
        createdAt
        updatedAt
    }
}
